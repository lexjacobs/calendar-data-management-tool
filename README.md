#### run development server
`yarn start`

#### run local node server (running latest compiled build)
`yarn watch`

#### run jest tests
`yarn test`

#### build
`yarn build`

#### deploy
`git push heroku master`
